services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nycCodeQuest-app-prod
    ports:
      - "5000:5000"
    env_file:
      - ./server/.env
    environment:
      - PORT=5000
      - NODE_ENV=production


